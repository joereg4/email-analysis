FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install fastapi uvicorn python-multipart

# Copy the minimal API
COPY minimal-api.py .

# Expose port
EXPOSE 8080

# Run the API
CMD ["python", "minimal-api.py"]

